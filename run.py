from app import app#import app instance

if __name__ == '__main__':
    app.run()       #We now remove the debug = True argument from our app.run() because the debug mode has been enabled in the configuration file.


